name: test

on:
  push:
    branches: [ "compress-test" ]
    paths-ignore:
      - '*.md'
      - '.gitignore'
      - 'scripts/packer/amd64'
  pull_request:
    branches: [ "compress-test" ]
    paths-ignore:
      - '*.md'
      - '.gitignore'
      - 'scripts/packer/amd64'
  workflow_dispatch:

jobs:
  arm64-rpi-image-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set values
        run: |
          echo "BUILD_DATE=$(date +"%Y-%m-%d")" >> $GITHUB_ENV
          echo "BUILD_VERSION=$(git describe --always --tags)" >> $GITHUB_ENV

      - name: Hello Joininbox
        run: echo "Building the joininbox-arm64-rpi-image-${{ env.BUILD_DATE }}-${{ env.BUILD_VERSION }}"

      - name: Run the build script
        run: |
          mkdir -p scripts/packer/arm64-rpi/packer-builder-arm
          cd scripts/packer/arm64-rpi/packer-builder-arm/
          echo test > joininbox-arm64-rpi.img

      - name: Compress image
        run: |
          cd scripts/packer/arm64-rpi/packer-builder-arm/
          tar -czvf joininbox-arm64-rpi.img.tar.gz joininbox-arm64-rpi.img

      - name: Compute checksum
        run: |
          cd scripts/packer/arm64-rpi/packer-builder-arm/
          sha256sum joininbox-arm64-rpi.img.tar.gz > joininbox-arm64-rpi.img.tar.gz.sha256

      - name: Upload the image
        uses: actions/upload-artifact@v3
        with:
          name: joininbox-arm64-rpi-image-${{ env.BUILD_DATE }}-${{ env.BUILD_VERSION }}
          path: |
            ${{ github.workspace }}/scripts/packer/arm64-rpi/packer-builder-arm/joininbox-arm64-rpi.img
            ${{ github.workspace }}/scripts/packer/arm64-rpi/packer-builder-arm/joininbox-arm64-rpi.img.sha256

